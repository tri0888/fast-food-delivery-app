# admin.compat.test

| ID | Test level | Mô tả test case | Inter-test case Dependence | Quy trình kiểm thử | Kết quả mong đợi | Dữ liệu kiểm thử | Kết quả |
| --- | --- | --- | --- | --- | --- | --- | --- |
| COMPAT-ADMIN-001 | Acceptance | Mở trang Users và hiển thị menu sidebar (cross-browser) | Không | 1. Set `sessionStorage.token = PLAYWRIGHT_ADMIN_TOKEN` trước khi vào trang<br>2. Stub tất cả `/api/**` trả `{ success: true, data: [] }` và (nếu có) verify request có header `token`<br>3. Mở `/users` trên Chromium/Firefox/WebKit<br>4. Quan sát sidebar links | Sidebar có các link `Add Items`, `List Items`, `Orders`, `Users`; không redirect về login | Token `'playwright-admin'`, base URL `PLAYWRIGHT_ADMIN_BASE_URL` | PASS |
| COMPAT-ADMIN-002 | Acceptance | Điều hướng giữa Users → Orders không crash (cross-browser) | COMPAT-ADMIN-001 | 1. Từ `/users` click link `Orders`<br>2. Đợi URL `/orders`<br>3. Refresh trang và kiểm tra vẫn render | URL đúng, trang render bình thường sau refresh, không console error nghiêm trọng | Token `'playwright-admin'` | PASS |
| COMPAT-ADMIN-003 | Acceptance | Điều hướng Users → List Items render bảng/list (cross-browser) | COMPAT-ADMIN-001 | 1. Click `List Items`<br>2. Đợi URL `/list`<br>3. Xác nhận có container list/table hiển thị | Có vùng danh sách items (table/list) và layout không vỡ | Token `'playwright-admin'` | PASS |
| COMPAT-ADMIN-004 | Acceptance | Trang Add Items render form và input cơ bản hoạt động (cross-browser) | COMPAT-ADMIN-001 | 1. Click `Add Items`<br>2. Đợi URL `/add`<br>3. Kiểm tra các input chính (Name/Price/Description/Stock) focus được và nhập được text | Form render, các input nhận focus/typing bình thường trên 3 browser | Token `'playwright-admin'` | PASS |
| COMPAT-ADMIN-005 | Acceptance | Route deep-link `/users` mở trực tiếp không cần đi qua trang khác (cross-browser) | Không | 1. Truy cập thẳng `/users` (fresh context) với token đã inject<br>2. Đợi DOM stable | Không redirect, trang users render ngay | Token `'playwright-admin'` | PASS |
